specVersion: "v1"
serviceId: "download"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "download-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
